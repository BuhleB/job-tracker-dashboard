# job-tracker-dashboard
A REST API built to track job applications through a structured status workflow: applied → interviewing → offer → accepted, with rejected and withdrawn available as exits at any active stage.
